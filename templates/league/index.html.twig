{% extends 'base.html.twig' %}
{% block body %}
	<div class="example-wrapper">
		<h1>{{ league }}</h1>
	</div>
	<h3>Players In League</h3>
	<ul>
		{% for user in users %}
			<li>{{ user.username }}
				- Score:
				{{user.total}}</li>
		{% endfor %}
	</ul>
{% endblock %}
